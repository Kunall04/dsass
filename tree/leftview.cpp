class Solution {
  public:
    vector<int> bfs(vector<vector<int>> &adj) {
        int n=adj.size();
        vector<int> visited(n,0);
        visited[0]=1;
        vector<int> bfs;
        queue<int> q;
        
        
    }
};